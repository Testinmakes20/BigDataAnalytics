The official binary archive aspectj-1.6.9.jar is an installer that needs to
be run manually with java -jar aspectj-1.6.9.jar. This installer puts
everything in a directory named aspectj1.6. This is then archived as
aspectj-1.6.9-installed.jar in compressed, and it is this archive that is
installed in bin.

The archive aspectj-src-1.6.9.jar contains aspectjrt1.6.9-src.jar,
aspectjtools1.6.9-src.jar, aspectjweaver1.6.9-src.jar, and
org.aspectj.matcher-1.6.9-src.jar, which contain the actual source.  It is
the contents of these archives that have been unpacked into the src
directory.  These source archives have been deleted after being unpacked.

To retrieve the original source jar archive, unpack it from 

unzip aspectj-src-1.6.9.jar -d [destination]


